<Window x:Class="BackupUI.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:local="clr-namespace:BackupUI.Converters"
Title="Backup &amp; Restore"
Height="600"
Width="900">
	<Window.Resources>
		<local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
	</Window.Resources>
	<DockPanel>
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_File">
				<MenuItem Header="_Exit"
						  Click="Exit_Click" />
			</MenuItem>
			<MenuItem Header="_Backup">
				<MenuItem Header="_New Backup..."
						  Click="NewBackup_Click" />
				<MenuItem Header="_Restore..."
						  Click="Restore_Click" />
			</MenuItem>
			<MenuItem Header="_Schedules">
				<MenuItem Header="_Manage Schedules..."
						  Click="ManageSchedules_Click" />
			</MenuItem>
			<MenuItem Header="_Activity">
				<MenuItem Header="_Activity Management..."
						  Click="ActivityManagement_Click" />
			</MenuItem>
			<MenuItem Header="_Service">
				<MenuItem Header="_Service Management..."
						  Click="ServiceManagement_Click" />
				<Separator />
				<MenuItem Header="_Recovery Environment Creator..."
						  Click="RecoveryEnvironmentCreator_Click" />
			</MenuItem>
			<MenuItem Header="_Help">
				<MenuItem Header="_About..."
						  Click="About_Click" />
			</MenuItem>
		</Menu>
		<StatusBar DockPanel.Dock="Bottom" Height="25">
			<StatusBarItem>
				<TextBlock Name="txtVersion" Text="Version: Loading..." FontSize="11" Foreground="#666"/>
			</StatusBarItem>
		</StatusBar>
		<Grid Margin="10">
			<TabControl Name="mainTabControl" SelectionChanged="TabControl_SelectionChanged">
				<TabItem Header="Backup">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="*"/>
						</Grid.RowDefinitions>

						<!-- Header -->
						<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,5" Background="#F0F0F0">
							<TextBlock Text="Backup Jobs" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="10,10,20,10"/>
							<Button Content="New Backup..." Click="NewBackup_Click" Width="120" Height="30" Margin="5,5"/>
							<Button Content="Import Backup..." Click="ImportBackup_Click" Width="130" Height="30" Margin="5,5"/>
							<Button Content="Refresh" Click="RefreshJobs_Click" Width="100" Height="30" Margin="5,5"/>
						</StackPanel>

						<!-- Job List -->
						<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
							<ItemsControl Name="lstBackupJobs">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border BorderBrush="#CCC" BorderThickness="1" Margin="0,0,0,10" Background="White" CornerRadius="5">
											<Grid Margin="15">
												<Grid.RowDefinitions>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
												</Grid.RowDefinitions>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="150"/>
													<ColumnDefinition Width="*"/>
													<ColumnDefinition Width="Auto"/>
												</Grid.ColumnDefinitions>

												<!-- Job Name -->
												<TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" 
														   Text="{Binding Name}" 
														   FontSize="16" FontWeight="Bold" 
														   Margin="0,0,0,10"/>

												<!-- Type -->
												<TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TypeDescription}" Margin="0,5"/>

												<!-- Source -->
												<TextBlock Grid.Row="2" Grid.Column="0" Text="Source:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SourceDescription}" Margin="0,5" TextWrapping="Wrap"/>

												<!-- Destination -->
												<TextBlock Grid.Row="3" Grid.Column="0" Text="Destination:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DestinationPath}" Margin="0,5" TextWrapping="Wrap"/>

												<!-- Schedule -->
												<TextBlock Grid.Row="4" Grid.Column="0" Text="Schedule:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ScheduleDescription}" Margin="0,5"/>

												<!-- Buttons -->
												<StackPanel Grid.Row="0" Grid.RowSpan="5" Grid.Column="2" 
														    Orientation="Vertical" 
														    VerticalAlignment="Center" 
														    Margin="20,0,0,0">
													<Button Content="Run Now" 
															Width="100" 
															Height="35" 
															Margin="0,5"
															Tag="{Binding Id}"
															Click="RunJobNow_Click"/>
													<Button Content="Edit" 
															Width="100" 
															Height="35" 
															Margin="0,5"
															Tag="{Binding Id}"
															Click="EditJob_Click"/>
													<Button Content="Delete" 
															Width="100" 
															Height="35" 
															Margin="0,5"
															Background="#FFE0E0"
															Tag="{Binding Id}"
															Click="DeleteJob_Click"/>
												</StackPanel>
											</Grid>
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</ScrollViewer>

						<!-- No Jobs Message -->
						<TextBlock Name="txtNoJobs"
								   Grid.Row="1"
								   Text="No backup jobs found. Click 'New Backup...' to create one."
								   HorizontalAlignment="Center"
								   VerticalAlignment="Center"
								   FontSize="14"
								   Foreground="#999"
								   Visibility="Collapsed"/>
					</Grid>
				</TabItem>

				<!-- Activity/Logs Tab -->
				<TabItem Name="tabActivity" Header="Activity">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="*"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>

						<!-- Header -->
						<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" Background="#F0F0F0" Height="50">
							<TextBlock Text="Backup Job Activity Logs" 
					   FontSize="18" 
					   FontWeight="Bold" 
					   VerticalAlignment="Center" 
					   Margin="15,0,20,0"/>
							<Button Content="Refresh" 
					Click="RefreshJobLogs_Click" 
					Width="100" 
					Height="32" 
					Margin="5"/>
							<Button Content="View All Activities" 
					Click="ViewAllActivitiesFromTab_Click" 
					Width="140" 
					Height="32" 
					Margin="5"/>
						</StackPanel>

						<!-- Job Logs List -->
						<DataGrid Grid.Row="1" 
				  Name="dgJobLogs" 
				  Margin="10"
				  AutoGenerateColumns="False" 
				  IsReadOnly="True"
				  SelectionMode="Single"
				  GridLinesVisibility="Horizontal"
				  AlternatingRowBackground="#F9F9F9"
				  CanUserResizeColumns="True"
				  MouseDoubleClick="JobLog_DoubleClickFromTab"
				  RowHeight="35">
							<DataGrid.Columns>
							<DataGridTextColumn Header="Job Name" Binding="{Binding JobName}" Width="180">
								<DataGridTextColumn.ElementStyle>
									<Style TargetType="TextBlock">
										<Setter Property="TextWrapping" Value="Wrap"/>
										<Setter Property="VerticalAlignment" Value="Center"/>
										<Setter Property="Padding" Value="5"/>
									</Style>
								</DataGridTextColumn.ElementStyle>
							</DataGridTextColumn>
							<DataGridTextColumn Header="Total" Binding="{Binding TotalActivities}" Width="70">
								<DataGridTextColumn.ElementStyle>
									<Style TargetType="TextBlock">
										<Setter Property="HorizontalAlignment" Value="Center"/>
										<Setter Property="VerticalAlignment" Value="Center"/>
									</Style>
								</DataGridTextColumn.ElementStyle>
							</DataGridTextColumn>
							<DataGridTextColumn Header="Last Activity" Binding="{Binding LastActivity, StringFormat='{}{0:MM/dd HH:mm}'}" Width="100">
								<DataGridTextColumn.ElementStyle>
									<Style TargetType="TextBlock">
										<Setter Property="VerticalAlignment" Value="Center"/>
									</Style>
								</DataGridTextColumn.ElementStyle>
							</DataGridTextColumn>
							<DataGridTextColumn Header="Success" Binding="{Binding SuccessCount}" Width="70">
									<DataGridTextColumn.ElementStyle>
										<Style TargetType="TextBlock">
											<Setter Property="Foreground" Value="Green"/>
											<Setter Property="FontWeight" Value="SemiBold"/>
											<Setter Property="HorizontalAlignment" Value="Center"/>
											<Setter Property="VerticalAlignment" Value="Center"/>
											<Style.Triggers>
												<DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
													<Setter Property="Foreground" Value="LightGreen"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</DataGridTextColumn.ElementStyle>
								</DataGridTextColumn>
							<DataGridTextColumn Header="Warning" Binding="{Binding WarningCount}" Width="70">
									<DataGridTextColumn.ElementStyle>
										<Style TargetType="TextBlock">
											<Setter Property="Foreground" Value="Orange"/>
											<Setter Property="FontWeight" Value="SemiBold"/>
											<Setter Property="HorizontalAlignment" Value="Center"/>
											<Setter Property="VerticalAlignment" Value="Center"/>
											<Style.Triggers>
												<DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
													<Setter Property="Foreground" Value="Yellow"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</DataGridTextColumn.ElementStyle>
								</DataGridTextColumn>
							<DataGridTextColumn Header="Error" Binding="{Binding ErrorCount}" Width="60">
									<DataGridTextColumn.ElementStyle>
										<Style TargetType="TextBlock">
											<Setter Property="Foreground" Value="Red"/>
											<Setter Property="FontWeight" Value="SemiBold"/>
											<Setter Property="HorizontalAlignment" Value="Center"/>
											<Setter Property="VerticalAlignment" Value="Center"/>
											<Style.Triggers>
												<DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
													<Setter Property="Foreground" Value="LightCoral"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</DataGridTextColumn.ElementStyle>
								</DataGridTextColumn>
							<DataGridTemplateColumn Header="Actions" Width="150">
									<DataGridTemplateColumn.CellTemplate>
										<DataTemplate>
										<Button Content="View Details" 
												Width="120" 
												Height="28" 
												Margin="5,0"
												HorizontalAlignment="Center"
												VerticalAlignment="Center"
												Tag="{Binding JobName}"
												Click="ViewJobDetailsFromTab_Click"/>
										</DataTemplate>
									</DataGridTemplateColumn.CellTemplate>
								</DataGridTemplateColumn>
							</DataGrid.Columns>
						</DataGrid>

						<!-- Footer Info -->
						<Border Grid.Row="2" 
				Background="#F0F0F0" 
				Padding="10" 
				BorderBrush="#CCC" 
				BorderThickness="1">
							<TextBlock Name="txtJobLogsStatus" 
					   Text="Double-click a job to view detailed activities" 
					   FontSize="12" 
					   Foreground="#666"/>
						</Border>
					</Grid>
				</TabItem>

				<!-- Mount Backups Tab -->
				<TabItem Header="Mount Backups">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="*"/>
							<RowDefinition Height="200"/>
						</Grid.RowDefinitions>

						<!-- Header -->
						<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,5" Background="#F0F0F0">
							<TextBlock Text="Mount Backups as Virtual Drives" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="10,10,20,10"/>
							<Button Content="Refresh" Click="RefreshMounts_Click" Width="100" Height="30" Margin="5,5"/>
							<Button Content="Unmount All" Click="UnmountAll_Click" Width="120" Height="30" Margin="5,5" Background="#FFE0E0"/>
						</StackPanel>

						<!-- Available Backups -->
						<GroupBox Grid.Row="1" Header="Available Backups" Margin="10">
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="*"/>
									<RowDefinition Height="Auto"/>
								</Grid.RowDefinitions>

								<DataGrid Grid.Row="0" Name="dgAvailableBackups" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" GridLinesVisibility="Horizontal" AlternatingRowBackground="#F9F9F9" SelectionChanged="AvailableBackups_SelectionChanged">
									<DataGrid.Columns>
										<DataGridTextColumn Header="Backup Name" Binding="{Binding BackupName}" Width="200"/>
										<DataGridTextColumn Header="Type" Binding="{Binding BackupType}" Width="100"/>
										<DataGridTextColumn Header="Date" Binding="{Binding BackupDate, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
										<DataGridTextColumn Header="Path" Binding="{Binding BackupPath}" Width="*"/>
										<DataGridTemplateColumn Header="Action" Width="100">
											<DataGridTemplateColumn.CellTemplate>
												<DataTemplate>
													<Button Content="Mount" Width="80" Height="25" Click="MountBackup_Click" Tag="{Binding}"/>
												</DataTemplate>
											</DataGridTemplateColumn.CellTemplate>
										</DataGridTemplateColumn>
									</DataGrid.Columns>
								</DataGrid>

								<StackPanel Grid.Row="1" Name="pnlBackupPoints" Visibility="Collapsed" Margin="10">
									<TextBlock Text="Select Backup Point:" FontWeight="SemiBold" Margin="0,0,0,5"/>
									<ComboBox Name="cmbBackupPoints" Height="30"/>
								</StackPanel>
							</Grid>
						</GroupBox>

						<!-- Mounted Backups -->
						<GroupBox Grid.Row="2" Header="Mounted Backups" Margin="10">
							<DataGrid Name="dgMountedBackups" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" GridLinesVisibility="Horizontal" AlternatingRowBackground="#F9F9F9">
								<DataGrid.Columns>
									<DataGridTextColumn Header="Drive" Binding="{Binding DriveLetter}" Width="60"/>
									<DataGridTextColumn Header="Backup Name" Binding="{Binding BackupName}" Width="200"/>
									<DataGridTextColumn Header="Type" Binding="{Binding BackupType}" Width="100"/>
									<DataGridTextColumn Header="Date" Binding="{Binding BackupDate, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
									<DataGridTextColumn Header="Mounted At" Binding="{Binding MountTime, StringFormat='{}{0:HH:mm:ss}'}" Width="100"/>
									<DataGridTemplateColumn Header="Status" Width="80">
										<DataGridTemplateColumn.CellTemplate>
											<DataTemplate>
												<TextBlock Text="Read-Only" Foreground="Green" HorizontalAlignment="Center" FontWeight="SemiBold"/>
											</DataTemplate>
										</DataGridTemplateColumn.CellTemplate>
									</DataGridTemplateColumn>
									<DataGridTemplateColumn Header="Action" Width="100">
										<DataGridTemplateColumn.CellTemplate>
											<DataTemplate>
												<Button Content="Unmount" Width="80" Height="25" Background="#FFE0E0" Click="UnmountBackup_Click" Tag="{Binding DriveLetter}"/>
											</DataTemplate>
										</DataGridTemplateColumn.CellTemplate>
									</DataGridTemplateColumn>
								</DataGrid.Columns>
							</DataGrid>
						</GroupBox>

						<TextBlock Name="txtNoBackups" Grid.Row="1" Text="No backups available to mount." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="#999" Visibility="Collapsed"/>
					</Grid>
				</TabItem>

				<TabItem Header="Restore">
					<TextBlock Text="Use File  Restore to restore from backup"
							   HorizontalAlignment="Center"
							   VerticalAlignment="Center" />
				</TabItem>
				<TabItem Header="Schedules">
					<StackPanel HorizontalAlignment="Center"
								VerticalAlignment="Center">
						<Button Content="Manage Schedules"
								Width="200"
								Height="40"
								Click="ManageSchedules_Click"
								Margin="0,0,0,10" />
						<Button Content="Service Management"
								Width="200"
								Height="40"
								Click="ServiceManagement_Click" />
					</StackPanel>
				</TabItem>
			</TabControl>
		</Grid>
	</DockPanel>
</Window>
