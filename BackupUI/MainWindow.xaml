<Window x:Class="BackupUI.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:local="clr-namespace:BackupUI.Converters"
Title="Backup &amp; Restore"
Height="600"
Width="900">
	<Window.Resources>
		<local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
	</Window.Resources>
	<DockPanel>
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_File">
				<MenuItem Header="_Exit"
						  Click="Exit_Click" />
			</MenuItem>
			<MenuItem Header="_Backup">
				<MenuItem Header="_New Backup..."
						  Click="NewBackup_Click" />
				<MenuItem Header="_Restore..."
						  Click="Restore_Click" />
			</MenuItem>
			<MenuItem Header="_Schedules">
				<MenuItem Header="_Manage Schedules..."
						  Click="ManageSchedules_Click" />
			</MenuItem>
			<MenuItem Header="_Service">
				<MenuItem Header="_Service Management..."
						  Click="ServiceManagement_Click" />
				<Separator />
				<MenuItem Header="_Recovery Environment Creator..."
						  Click="RecoveryEnvironmentCreator_Click" />
			</MenuItem>
			<MenuItem Header="_Help">
				<MenuItem Header="_About..."
						  Click="About_Click" />
			</MenuItem>
		</Menu>
		<StatusBar DockPanel.Dock="Bottom" Height="25">
			<StatusBarItem>
				<TextBlock Name="txtVersion" Text="Version: Loading..." FontSize="11" Foreground="#666"/>
			</StatusBarItem>
		</StatusBar>
		<Grid Margin="10">
			<TabControl Name="mainTabControl" SelectionChanged="TabControl_SelectionChanged">
				<TabItem Header="Backup">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="*"/>
						</Grid.RowDefinitions>
						
						<!-- Header -->
						<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,5" Background="#F0F0F0">
							<TextBlock Text="Backup Jobs" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="10,10,20,10"/>
							<Button Content="New Backup..." Click="NewBackup_Click" Width="120" Height="30" Margin="5,5"/>
							<Button Content="Refresh" Click="RefreshJobs_Click" Width="100" Height="30" Margin="5,5"/>
						</StackPanel>
						
						<!-- Job List -->
						<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
							<ItemsControl Name="lstBackupJobs">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border BorderBrush="#CCC" BorderThickness="1" Margin="0,0,0,10" Background="White" CornerRadius="5">
											<Grid Margin="15">
												<Grid.RowDefinitions>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
													<RowDefinition Height="Auto"/>
												</Grid.RowDefinitions>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="150"/>
													<ColumnDefinition Width="*"/>
													<ColumnDefinition Width="Auto"/>
												</Grid.ColumnDefinitions>
												
												<!-- Job Name -->
												<TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" 
														   Text="{Binding Name}" 
														   FontSize="16" FontWeight="Bold" 
														   Margin="0,0,0,10"/>
												
												<!-- Type -->
												<TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TypeDescription}" Margin="0,5"/>
												
												<!-- Source -->
												<TextBlock Grid.Row="2" Grid.Column="0" Text="Source:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SourceDescription}" Margin="0,5" TextWrapping="Wrap"/>
												
												<!-- Destination -->
												<TextBlock Grid.Row="3" Grid.Column="0" Text="Destination:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DestinationPath}" Margin="0,5" TextWrapping="Wrap"/>
												
												<!-- Schedule -->
												<TextBlock Grid.Row="4" Grid.Column="0" Text="Schedule:" FontWeight="SemiBold" Margin="0,5"/>
												<TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ScheduleDescription}" Margin="0,5"/>
												
												<!-- Buttons -->
												<StackPanel Grid.Row="0" Grid.RowSpan="5" Grid.Column="2" 
														    Orientation="Vertical" 
														    VerticalAlignment="Center" 
														    Margin="20,0,0,0">
													<Button Content="Run Now" 
															Width="100" 
															Height="35" 
															Margin="0,5"
															Tag="{Binding Id}"
															Click="RunJobNow_Click"/>
													<Button Content="Edit" 
															Width="100" 
															Height="35" 
															Margin="0,5"
															Tag="{Binding Id}"
															Click="EditJob_Click"/>
													<Button Content="Delete" 
															Width="100" 
															Height="35" 
															Margin="0,5"
															Background="#FFE0E0"
															Tag="{Binding Id}"
															Click="DeleteJob_Click"/>
												</StackPanel>
											</Grid>
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</ScrollViewer>
						
						<!-- No Jobs Message -->
						<TextBlock Name="txtNoJobs"
								   Grid.Row="1"
								   Text="No backup jobs found. Click 'New Backup...' to create one."
								   HorizontalAlignment="Center"
								   VerticalAlignment="Center"
								   FontSize="14"
								   Foreground="#999"
								   Visibility="Collapsed"/>
					</Grid>
				</TabItem>
				
				<!-- Activity/Logs Tab -->
				<TabItem Name="tabActivity" Header="Activity">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="*"/>
						</Grid.RowDefinitions>
						
						<!-- Header with Controls -->
						<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,5" Background="#F0F0F0">
							<TextBlock Text="Backup Activity Log" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="10,10,20,10"/>
							<Button Content="Refresh" Click="RefreshActivity_Click" Width="100" Height="30" Margin="5,5"/>
							<Button Content="Clear Old Logs (30+ days)" Click="ClearOldLogs_Click" Width="180" Height="30" Margin="5,5"/>
							<ComboBox Name="cmbFilterLevel" Width="120" Height="30" Margin="5,5" SelectionChanged="FilterLevel_Changed">
								<ComboBoxItem Content="All" IsSelected="True"/>
								<ComboBoxItem Content="Info"/>
								<ComboBoxItem Content="Success"/>
								<ComboBoxItem Content="Warning"/>
								<ComboBoxItem Content="Error"/>
								<ComboBoxItem Content="Failed Validations"/>
							</ComboBox>
						</StackPanel>
						
						<!-- Activity Log DataGrid -->
						<DataGrid Grid.Row="1" 
								  Name="dgActivityLog" 
								  Margin="10"
								  AutoGenerateColumns="False" 
								  IsReadOnly="True"
								  SelectionMode="Single"
								  GridLinesVisibility="Horizontal"
								  AlternatingRowBackground="#F9F9F9"
								  CanUserResizeColumns="True">
							<DataGrid.Columns>
								<DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='{}{0:MM/dd/yyyy HH:mm:ss}'}" Width="140"/>
								<DataGridTextColumn Header="Job Name" Binding="{Binding JobName}" Width="150"/>
								<DataGridTemplateColumn Header="Level" Width="80">
									<DataGridTemplateColumn.CellTemplate>
										<DataTemplate>
											<Border Padding="5,2" CornerRadius="3">
												<Border.Style>
													<Style TargetType="Border">
														<Style.Triggers>
															<DataTrigger Binding="{Binding Level}" Value="Info">
																<Setter Property="Background" Value="#E3F2FD"/>
															</DataTrigger>
															<DataTrigger Binding="{Binding Level}" Value="Success">
																<Setter Property="Background" Value="#E8F5E9"/>
															</DataTrigger>
															<DataTrigger Binding="{Binding Level}" Value="Warning">
																<Setter Property="Background" Value="#FFF3E0"/>
															</DataTrigger>
															<DataTrigger Binding="{Binding Level}" Value="Error">
																<Setter Property="Background" Value="#FFEBEE"/>
															</DataTrigger>
														</Style.Triggers>
													</Style>
												</Border.Style>
												<TextBlock Text="{Binding Level}" HorizontalAlignment="Center" FontWeight="SemiBold"/>
											</Border>
										</DataTemplate>
									</DataGridTemplateColumn.CellTemplate>
								</DataGridTemplateColumn>
								<DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
								<DataGridTemplateColumn Header="Validation" Width="80">
									<DataGridTemplateColumn.CellTemplate>
										<DataTemplate>
											<TextBlock HorizontalAlignment="Center">
												<TextBlock.Style>
													<Style TargetType="TextBlock">
														<Setter Property="Text" Value="?"/>
														<Setter Property="Foreground" Value="Green"/>
														<Setter Property="FontWeight" Value="Bold"/>
														<Style.Triggers>
															<DataTrigger Binding="{Binding ValidationPassed}" Value="False">
																<Setter Property="Text" Value="?"/>
																<Setter Property="Foreground" Value="Red"/>
															</DataTrigger>
														</Style.Triggers>
													</Style>
												</TextBlock.Style>
											</TextBlock>
										</DataTemplate>
									</DataGridTemplateColumn.CellTemplate>
								</DataGridTemplateColumn>
							</DataGrid.Columns>
							
							<!-- Row Details for full information -->
							<DataGrid.RowDetailsTemplate>
								<DataTemplate>
									<Border Background="#F5F5F5" Padding="10" Margin="5">
										<StackPanel>
											<TextBlock Text="Details:" FontWeight="Bold" Margin="0,0,0,5"/>
											<TextBlock Text="{Binding Details}" TextWrapping="Wrap" Margin="0,0,0,10"/>
											<TextBlock Text="{Binding BackupPath, StringFormat='Backup Path: {0}'}" 
													   TextWrapping="Wrap"
													   Visibility="{Binding BackupPath, Converter={StaticResource StringToVisibilityConverter}}"/>
										</StackPanel>
									</Border>
								</DataTemplate>
							</DataGrid.RowDetailsTemplate>
						</DataGrid>
						
						<!-- No Logs Message -->
						<TextBlock Name="txtNoLogs"
								   Grid.Row="1"
								   Text="No activity logs found."
								   HorizontalAlignment="Center"
								   VerticalAlignment="Center"
								   FontSize="14"
								   Foreground="#999"
								   Visibility="Collapsed"/>
					</Grid>
				</TabItem>
				
				<TabItem Header="Restore">
					<TextBlock Text="Use File  Restore to restore from backup"
							   HorizontalAlignment="Center"
							   VerticalAlignment="Center" />
				</TabItem>
				<TabItem Header="Schedules">
					<StackPanel HorizontalAlignment="Center"
								VerticalAlignment="Center">
						<Button Content="Manage Schedules"
								Width="200"
								Height="40"
								Click="ManageSchedules_Click"
								Margin="0,0,0,10" />
						<Button Content="Service Management"
								Width="200"
								Height="40"
								Click="ServiceManagement_Click" />
					</StackPanel>
				</TabItem>
			</TabControl>
		</Grid>
	</DockPanel>
</Window>