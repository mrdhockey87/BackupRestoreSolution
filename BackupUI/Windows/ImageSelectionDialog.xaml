<Window x:Class="BackupUI.Windows.ImageSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Restore Point" 
        Height="400" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Multiple Restore Points Available" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryText}"/>
            <TextBlock Text="This backup contains multiple restore points. Select which point to mount:"
                       Margin="0,5,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource SecondaryText}"/>
        </StackPanel>

        <!-- Image List -->
        <Border Grid.Row="1" 
                BorderBrush="{DynamicResource LightBorderBrush}" 
                BorderThickness="1"
                Background="White">
            <DataGrid x:Name="dgImages"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      CanUserResizeRows="False"
                      CanUserReorderColumns="False"
                      AlternatingRowBackground="{DynamicResource AlternateRowBackground}"
                      MouseDoubleClick="dgImages_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Image #" 
                                        Binding="{Binding ImageIndex}" 
                                        Width="80"
                                        ElementStyle="{StaticResource CenterAlignedCell}"/>
                    <DataGridTextColumn Header="Date/Time" 
                                        Binding="{Binding ImageDate, StringFormat='MM/dd/yyyy HH:mm:ss'}" 
                                        Width="180"/>
                    <DataGridTextColumn Header="Type" 
                                        Binding="{Binding ImageType}" 
                                        Width="120"/>
                    <DataGridTextColumn Header="Description" 
                                        Binding="{Binding Description}" 
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="Mount Selected" 
                    Width="120" 
                    Height="32"
                    Margin="0,0,10,0"
                    Style="{StaticResource SuccessButton}"
                    Click="btnMount_Click"/>
            <Button Content="Cancel" 
                    Width="100" 
                    Height="32"
                    Style="{StaticResource SecondaryButton}"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
