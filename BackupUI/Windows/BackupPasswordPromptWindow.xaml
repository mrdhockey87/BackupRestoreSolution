<Window x:Class="BackupUI.Windows.BackupPasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup Password"
        Width="420"
        Height="210"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter the password for this encrypted backup."
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="Password:" Margin="0,0,0,5"/>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <PasswordBox x:Name="pwdPassword" Grid.Column="0" Height="28" PasswordChanged="PasswordBox_PasswordChanged"/>
            <TextBox x:Name="txtPasswordVisible" Grid.Column="0" Height="28" Visibility="Collapsed" TextChanged="VisiblePassword_TextChanged"/>
            <CheckBox x:Name="chkShowPassword"
                      Grid.Column="1"
                      Margin="10,0,0,0"
                      VerticalAlignment="Center"
                      Content="Show"
                      Checked="ShowPassword_CheckedChanged"
                      Unchecked="ShowPassword_CheckedChanged"/>
        </Grid>

        <TextBlock x:Name="txtError"
                   Grid.Row="3"
                   Foreground="DarkRed"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Width="90" Margin="0,0,10,0" Content="OK" Click="Ok_Click"/>
            <Button Width="90" Content="Cancel" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
