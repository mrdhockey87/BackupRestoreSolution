<Window x:Class="BackupUI.Windows.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Secure Server Backup"
        Height="400"
        Width="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Border Background="{DynamicResource LightTurquoise}"
            BorderBrush="{DynamicResource PrimaryTurquoise}"
            BorderThickness="2"
            CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- Logo - adaptive sizing based on window DPI -->
            <Viewbox Grid.Row="0" 
                     Stretch="Uniform"
                     MaxHeight="200"
                     MaxWidth="200"
                     Margin="20">
                <Image x:Name="imgLogo"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            </Viewbox>

            <!-- Application Title -->
            <TextBlock Grid.Row="1"
                       Text="Secure Server Backup"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTurquoise}"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,5"/>

            <!-- Version and Loading -->
            <StackPanel Grid.Row="2"
                        Orientation="Vertical"
                        HorizontalAlignment="Center"
                        Margin="0,5,0,20">
                <TextBlock x:Name="txtVersion"
                           Text="Version 5.13.11.4"
                           FontSize="14"
                           Foreground="{DynamicResource SecondaryText}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                
                <TextBlock x:Name="txtStatus"
                           Text="Loading..."
                           FontSize="12"
                           Foreground="{DynamicResource SecondaryText}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
            </StackPanel>

            <!-- Progress Bar -->
            <ProgressBar Grid.Row="3"
                         x:Name="progressBar"
                         Height="8"
                         Margin="40,0,40,20"
                         IsIndeterminate="True"
                         Foreground="{DynamicResource PrimaryTurquoise}"
                         Background="{DynamicResource LightBorderBrush}"/>
        </Grid>
    </Border>
</Window>
