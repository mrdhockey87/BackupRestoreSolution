<Window x:Class="BackupUI.Windows.NetworkPathDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Network Path" 
        Height="200" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Enter Network Path (UNC Format):"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="txtNetworkPath" 
                 Grid.Row="1"
                 Height="30"
                 FontSize="14"
                 VerticalContentAlignment="Center"
                 Margin="0,0,0,10"
                 Text="\\"/>

        <TextBlock Grid.Row="2"
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   Margin="0,0,0,10">
            <Run Text="Examples:"/>
            <LineBreak/>
            <Run Text="  \\server\share"/>
            <LineBreak/>
            <Run Text="  \\192.168.1.100\backups"/>
            <LineBreak/>
            <Run Text="  \\NAS\Documents"/>
        </TextBlock>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" 
                    Width="80" 
                    Height="30"
                    IsDefault="True"
                    Click="OK_Click"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30"
                    IsCancel="True"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
