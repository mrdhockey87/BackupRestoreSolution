<Window x:Class="BackupUI.Windows.ActivityManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Management - Backup Job Logs"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" Background="{StaticResource PanelBackground}" Height="50">
            <TextBlock Text="Backup Job Activity Logs" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center" 
                       Margin="15,0,20,0"/>
            <Button Content="Refresh" 
                    Click="Refresh_Click" 
                    Width="100" 
                    Height="32" 
                    Margin="5"/>
            <Button Content="View All Activities" 
                    Click="ViewAllActivities_Click" 
                    Width="140" 
                    Height="32" 
                    Margin="5"/>
        </StackPanel>

        <!-- Job Logs List -->
        <DataGrid Grid.Row="1" 
                  Name="dgJobLogs" 
                  Margin="0,0,0,10"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9F9F9"
                  CanUserResizeColumns="True"
                  MouseDoubleClick="JobLog_DoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Job Name" Binding="{Binding JobName}" Width="170"/>
                <DataGridTextColumn Header="Total Activities" Binding="{Binding TotalActivities}" Width="120" HeaderStringFormat="Total Activities"/>
                <DataGridTextColumn Header="Last Activity" Binding="{Binding LastActivity, StringFormat='{}{0:MM/dd/yyyy HH:mm:ss}'}" Width="150"/>
                <DataGridTextColumn Header="Success Count" Binding="{Binding SuccessCount}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Green"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Warning Count" Binding="{Binding WarningCount}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Orange"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Error Count" Binding="{Binding ErrorCount}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Actions" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="View Details" 
                                        Width="100" 
                                        Height="25" 
                                        Margin="5,0"
                                        Tag="{Binding JobName}"
                                        Click="ViewJobDetails_Click"/>
                                <Button Content="Export" 
                                        Width="80" 
                                        Height="25" 
                                        Margin="5,0"
                                        Tag="{Binding JobName}"
                                        Click="ExportJobLog_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer Info -->
        <Border Grid.Row="2" 
                Background="{StaticResource PanelBackground}" 
                Padding="10" 
                BorderBrush="{StaticResource LightBorderBrush}" 
                BorderThickness="1">
            <TextBlock Name="txtStatus" 
                       Text="Double-click a job to view detailed activities" 
                       FontSize="12" 
                       Foreground="{StaticResource SecondaryText}"/>
        </Border>
    </Grid>
</Window>
