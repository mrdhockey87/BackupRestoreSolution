<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================================================================
         TURQUOISE THEME COLOR PALETTE
         ================================================================
         All colors explicitly defined to prevent Windows dark mode overrides.
         Color scheme: Turquoise (blue-green) with black text.
         ================================================================ -->

    <!-- Primary Turquoise Colors -->
    <SolidColorBrush x:Key="PrimaryTurquoise" Color="#20B2AA"/><!-- Light Sea Green -->
    <SolidColorBrush x:Key="DarkTurquoise" Color="#00CED1"/><!-- Dark Turquoise -->
    <SolidColorBrush x:Key="MediumTurquoise" Color="#48D1CC"/><!-- Medium Turquoise -->
    <SolidColorBrush x:Key="LightTurquoise" Color="#AFEEEE"/><!-- Pale Turquoise -->
    <SolidColorBrush x:Key="VeryLightTurquoise" Color="#E0F7F7"/><!-- Very Light Turquoise -->

    <!-- Button Colors -->
    <SolidColorBrush x:Key="ButtonBackground" Color="#008B8B"/><!-- Dark Cyan (Dark Turquoise for buttons) -->
    <SolidColorBrush x:Key="ButtonForeground" Color="#000000"/><!-- Black -->
    <SolidColorBrush x:Key="ButtonHover" Color="#20B2AA"/><!-- Light Sea Green -->
    <SolidColorBrush x:Key="ButtonPressed" Color="#5F9EA0"/><!-- Cadet Blue -->

    <!-- Text Colors -->
    <SolidColorBrush x:Key="PrimaryText" Color="#000000"/><!-- Black -->
    <SolidColorBrush x:Key="SecondaryText" Color="#333333"/><!-- Dark Gray -->
    <SolidColorBrush x:Key="ErrorText" Color="#8B0000"/><!-- Dark Red -->
    <SolidColorBrush x:Key="WarningText" Color="#FF8C00"/><!-- Dark Orange -->
    <SolidColorBrush x:Key="SuccessText" Color="#006400"/><!-- Dark Green -->
    <SolidColorBrush x:Key="InfoText" Color="#000080"/><!-- Navy Blue -->

    <!-- Background Colors -->
    <SolidColorBrush x:Key="WindowBackground" Color="#F5FFFF"/><!-- Very light turquoise tint -->
    <SolidColorBrush x:Key="PanelBackground" Color="#E0F7F7"/><!-- Light turquoise -->
    <SolidColorBrush x:Key="AlternateRowBackground" Color="#F0FAFA"/><!-- Very light turquoise -->
    <SolidColorBrush x:Key="HeaderBackground" Color="#B0E0E6"/><!-- Powder Blue -->
    <SolidColorBrush x:Key="StatusBarBackground" Color="#AFEEEE"/><!-- Pale Turquoise -->

    <!-- Border Colors -->
    <SolidColorBrush x:Key="BorderBrush" Color="#5F9EA0"/><!-- Cadet Blue -->
    <SolidColorBrush x:Key="LightBorderBrush" Color="#B0E0E6"/><!-- Powder Blue -->

    <!-- Selection Colors -->
    <SolidColorBrush x:Key="SelectionBackground" Color="#008B8B"/><!-- Dark Cyan -->
    <SolidColorBrush x:Key="SelectionForeground" Color="#FFFFFF"/><!-- White for contrast on dark turquoise -->

    <!-- Status-specific Colors -->
    <SolidColorBrush x:Key="SuccessBackground" Color="#E6F4EA"/><!-- Very light green tint -->
    <SolidColorBrush x:Key="WarningBackground" Color="#FFF8DC"/><!-- Cornsilk -->
    <SolidColorBrush x:Key="ErrorBackground" Color="#FFE4E1"/><!-- Misty Rose -->
    <SolidColorBrush x:Key="InfoBackground" Color="#E0F7F7"/><!-- Light Turquoise -->

    <!-- ================================================================
         BUTTON STYLES
         ================================================================ -->
    <Style x:Key="TurquoiseButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default Button Style -->
    <Style TargetType="Button" BasedOn="{StaticResource TurquoiseButton}"/>

    <!-- ================================================================
         WINDOW STYLE
         ================================================================ -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WindowBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         TEXTBLOCK STYLE
         ================================================================ -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         DATAGRID STYLE
         ================================================================ -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource AlternateRowBackground}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource LightBorderBrush}"/>
    </Style>

    <!-- DataGrid Column Header Style -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource HeaderBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
    </Style>

    <!-- DataGrid Cell Style - explicitly set to prevent dark mode override -->
    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource SelectionBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource SelectionForeground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid Row Style -->
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource SelectionBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource SelectionForeground}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource LightTurquoise}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ================================================================
         MENU STYLE
         ================================================================ -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         STATUSBAR STYLE
         ================================================================ -->
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource StatusBarBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         TEXTBOX STYLE
         ================================================================ -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5,3"/>
    </Style>

    <!-- ================================================================
         COMBOBOX STYLE
         ================================================================ -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ================================================================
         CHECKBOX STYLE
         ================================================================ -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         RADIOBUTTON STYLE
         ================================================================ -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         LABEL STYLE
         ================================================================ -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
    </Style>

    <!-- ================================================================
         TABCONTROL STYLE
         ================================================================ -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border Name="Border"
                            Background="{StaticResource VeryLightTurquoise}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1,1,1,0"
                            CornerRadius="3,3,0,0"
                            Padding="10,5">
                        <ContentPresenter ContentSource="Header"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource MediumTurquoise}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource LightTurquoise}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================================================================
         SCROLLVIEWER STYLE
         ================================================================ -->
    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- ================================================================
         BORDER STYLE
         ================================================================ -->
    <Style TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <!-- ================================================================
         LISTBOX STYLE
         ================================================================ -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource SelectionBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource SelectionForeground}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource LightTurquoise}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ================================================================
         TREEVIEW STYLE
         ================================================================ -->
    <Style TargetType="TreeView">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource SelectionBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource SelectionForeground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ================================================================
         EXPANDER STYLE
         ================================================================ -->
    <Style TargetType="Expander">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="Background" Value="{StaticResource VeryLightTurquoise}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <!-- ================================================================
         GROUPBOX STYLE
         ================================================================ -->
    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="Background" Value="{StaticResource VeryLightTurquoise}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ================================================================
         PROGRESSBAR STYLE
         ================================================================ -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource VeryLightTurquoise}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="20"/>
    </Style>

    <!-- ================================================================
         SPECIAL BUTTON STYLES
         ================================================================ -->
    
    <!-- Delete/Remove Button (Dark Red) -->
    <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource TurquoiseButton}">
        <Setter Property="Background" Value="#8B0000"/><!-- Dark Red -->
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Success/Confirm Button (Dark Green accent) -->
    <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource TurquoiseButton}">
        <Setter Property="Background" Value="#20B2AA"/><!-- Light Sea Green -->
    </Style>

    <!-- Warning Button -->
    <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource TurquoiseButton}">
        <Setter Property="Background" Value="#FF8C00"/><!-- Dark Orange -->
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Neutral/Secondary Button (Lighter Turquoise) -->
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource TurquoiseButton}">
        <Setter Property="Background" Value="#48D1CC"/><!-- Medium Turquoise -->
    </Style>

    <!-- ================================================================
         TOOLTIP STYLE
         ================================================================ -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource VeryLightTurquoise}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <!-- ================================================================
         CONTEXTMENU STYLE
         ================================================================ -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

</ResourceDictionary>
